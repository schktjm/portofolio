{
  "name": "portofolio",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node app.js",
    "minify": "uglifyjs ./public/app.js -c --source-map --output ./public/app.min.js",
    "start": "npm run minify && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "html-minifier": "^4.0.0",
    "prettier": "^1.18.2",
    "uglify-js": "^3.6.0"
  },
  "devDependencies": {
    "clean-css-cli": "^4.3.0"
  }
}
